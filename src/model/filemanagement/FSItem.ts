class FSItem {
    name: String
    path: String
    isRemote: boolean

    constructor(name: String, path: String, isRemote: boolean) {
        this.name = name
        this.path = path
        this.isRemote = isRemote
    }
}
